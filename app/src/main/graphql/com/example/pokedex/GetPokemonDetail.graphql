query GetPokemonDetail($name: String!) {
    pokemon(name: $name) {
        id
        name
        height
        weight
        types{
            type{
                name
            }
        }
        sprites {
            front_default
        }
    }
}
